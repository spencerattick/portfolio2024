/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn-images-1.medium.com', 'i.gr-assets.com', 'images-na.ssl-images-amazon.com'],
      },
};

export default nextConfig;
